{
  "name": "agent",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "bin": "index.js",
  "assets": [".env"],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && babel index.js -o ./dist/index.js",
    "dev": "nodemon --exec babel-node index.js",
    "package": "npm run build && pkg ./dist/index.js --out-path ./dist -t node8-win-x64"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.4.0",
    "@babel/preset-env": "^7.4.2",
    "nodemon": "^1.18.10"
  },
  "dependencies": {
    "dotenv": "^7.0.0",
    "socket.io-client": "^2.2.0"
  }
}
